# H3C 云桌面智能运维助手 — 产品需求文档（PRD v1.0.0）

> 文档版本：v1.0.0
> 产品经理：许清楚（Xu）
> 主理人：齐活林
> 最后更新：2026-09-08

---

## 1. 产品目标

H3C 云桌面智能运维助手是一款运行在 Windows 桌面端的本地化运维工具，面向 H3C Workspace（E2010）云桌面管理员与运维人员。它通过 H3C 官方 Workspace REST API 实时拉取告警与桌面/主机性能数据，并由大语言模型（OpenAI 兼容接口，可热插拔 DeepSeek/GLM/通义等）提供告警解释、故障排查建议与智能问答。结合 IMA 共享知识库 RAG 检索与联网搜索兜底，实现「告警可解释、健康可观测、问题可追溯」的轻量化智能运维闭环，所有运行文件、日志、配置、更新均落在 `C:\Users\liwanghao\Desktop\AI工具文件\v1.0.0\` 下，支持按版本号自动管理目录与离线可用。

---

## 2. 用户故事

**模块一 智能告警中心**
- 作为运维人员，我希望打开应用就能看到实时告警列表并按紧急/重要/次要/警告分级高亮展示，以便第一时间掌握集群异常概况。
- 作为运维人员，我希望点击某条告警后由 AI 直接给出「故障含义解释 + 3 条排查/解决建议」，以便无需翻阅手册即可快速决策。

**模块二 桌面健康监控**
- 作为管理员，我希望以表格一览所有云桌面的名称、IP、操作系统、状态及所属宿主机的 CPU/内存/磁盘利用率，以便快速发现性能瓶颈与异常桌面。
- 作为管理员，我希望性能数据能一键刷新并标注利用率过高的红色预警，以便及时干预过载宿主机。

**模块三 知识库与联网分析**
- 作为运维人员，我希望 AI 在分析告警或回答问题时先去 IMA 知识库做向量检索并将命中内容作为上下文，以便答案贴合本单位运维经验。
- 作为运维人员，我希望当知识库与模型都无法确定时自动联网搜索并汇总生成报告，以便在没有现成知识时仍能获得可靠参考。

**模块四 版本更新与软件库**
- 作为用户，我希望软件启动时自动在固定路径下按版本号创建/定位 `v1.0.0` 子目录并管理 logs/config/knowledge/updates，以便所有产物可控、可追溯。
- 作为用户，我希望应用能检测新版本并引导我把更新包下载到 updates/ 后重启完成升级，以便低成本保持工具最新。

---

## 3. 需求池（P0 / P1 / P2）

| 编号 | 功能点 | 优先级 | 验收标准 | 模块 |
|---|---|---|---|---|
| F1-01 | 平台连接配置（地址/账号/密码/端口） | P0 | 默认 `http://10.1.1.201:8083`；可在 UI 与 `config/` 配置文件填写与保存；保存后重启/重连生效 | 通用 |
| F1-02 | 摘要认证(Digest)登录 H3C REST | P0 | 使用「外部接口认证用户」管理员账号调用接口；认证失败返回 401 时给出明确提示并可重新配置 | 通用 |
| F1-03 | 实时告警列表拉取（2.12.1） | P0 | `GET /vdi/rest/workspace/realtimeAlarms/list`，必填 limit/offset/sortDir(1 ASC/2 DESC)/sortField；返回 `RpcPagingLoadResult<WarnInfoDTO>` 正确解析 data 数组 | 一 |
| F1-04 | 告警分级展示 | P0 | eventLevel 1=紧急/2=重要/3=次要/4=警告（编码按文档 4=警告，UI 文案可自定义为「提示」）；按等级配色高亮 | 一 |
| F1-05 | 告警详情 + AI 建议 | P0 | 点击告警调用模型，Prompt：「你是一名资深云桌面运维专家，请分析告警[名称/描述/级别]并给出3条建议」；返回解释+3条建议 | 一 |
| F1-06 | 告警筛选/排序 | P1 | 支持按 eventLevel、category、eventDesc、eventSrc、eventTime_from/to、state 等可选参数过滤 | 一 |
| F2-01 | 桌面列表与概要查询（2.9.11） | P0 | 遍历桌面 ID 逐个调用 `/virtual/domain/{id}/summary`，解析 title/osVersion/status、server.hostId、server.addresses[*].addr | 二 |
| F2-02 | 宿主机性能查询（2.4.12） | P0 | 用 hostId 调用 `/hosts/{id}/cpumemdiskrate`，解析 cpuRate/memRate/disk[] | 二 |
| F2-03 | 桌面健康表格聚合展示 | P0 | 表格列：名称、IP、操作系统、状态、宿主机CPU%、内存%、磁盘%（取必要磁盘利用率）；利用率超阈值（默认 CPU/内存≥85%）红色标注 | 二 |
| F2-04 | 一键刷新性能数据 | P1 | 手动刷新；刷新中禁用重复点击；展示最后刷新时间 | 二 |
| F3-01 | 关键词提取 | P0 | AI 分析告警/提问前先提取关键词（可由 LLM 或规则提取） | 三 |
| F3-02 | IMA 知识库 RAG 检索（本地检索方式） | P0 | 调用「ima 技能本地检索」适配层做向量检索；命中结果作为 Context 拼接进大模型请求 | 三 |
| F3-03 | RAG 适配层可替换 | P0 | 检索实现抽象为适配层接口；除 IMA 外，提供本地知识库文件兜底（knowledge/ 下文档向量化离线检索），保证离线可用 | 三 |
| F3-04 | 自动联网搜索兜底 | P1 | 当模型+IMA 均无法给出可信答案时，自动触发联网搜索并汇总生成报告 | 三 |
| F3-05 | 智能问答入口 | P1 | 提供输入框，用户提问 → 关键词 → RAG → 模型 →（必要时联网）回答 | 三 |
| F4-01 | 固定目录结构初始化 | P0 | 启动按版本号 `v1.0.0` 定位/创建 `C:\Users\liwanghao\Desktop\AI工具文件\v1.0.0\`，含 logs/、config/、knowledge/、updates/ | 四 |
| F4-02 | 版本号检测与更新检查 | P1 | 检查远程/指定路径最新版本号；有新版本提示用户 | 四 |
| F4-03 | 更新包下载与重启引导 | P1 | 新版本下载到 `updates/v{新版本}/`；提示并支持重启完成更新；启动自动识别最高版本目录 | 四 |
| F4-04 | 日志与配置落盘 | P0 | 所有日志写入 logs/；配置写入 config/；知识库文件存放 knowledge/ | 四 |
| F5-01 | LLM 接口配置（OpenAI 兼容） | P0 | 配置 base_url 与 api_key；支持热插拔 DeepSeek/GLM/通义等；默认模型可配置 | 通用 |
| F5-02 | Windows 桌面应用打包 | P0 | PyQt5 开发，PyInstaller 打包为 .exe，双击可运行，无需额外环境 | 通用 |

> 优先级说明：P0=版本必交付；P1=强烈建议本版本；P2=后续迭代（本表未列，可在评审中补充）。

---

## 4. UI 设计稿（文字描述）

### 4.1 主窗口布局
采用「**左侧导航 + 右侧内容区**」结构，顶部为全局状态栏。

```
┌───────────────────────────────────────────────────────────┐
│  H3C 云桌面智能运维助手  v1.0.0   [连接状态:●已连接] [设置⚙] │  ← 顶部状态栏
├──────────┬────────────────────────────────────────────────┤
│  导航     │                                                   │
│  🔔告警    │              （右侧随导航切换的内容区）            │
│  🖥健康    │                                                   │
│  📚知识库  │                                                   │
│  ⬆更新    │                                                   │
│           │                                                   │
│           │                                                   │
└──────────┴────────────────────────────────────────────────┘
```

- 顶部状态栏：显示当前版本号、H3C 平台连接状态（绿点/红点）、设置入口。
- 左侧导航：四个模块菜单（告警中心 / 桌面健康 / 知识库与联网 / 版本更新）。

### 4.2 模块一 智能告警中心（内容区）
```
┌─ 筛选栏 ──────────────────────────────────────────────┐
│ 等级[全部▾] 关键字[______] 时间[起]~[止] [查询][自动刷新▾]│
├───────────────────────────────────────────────────────┤
│ 级别    告警名称              时间            状态       │
│ [紧急] 主机CPU使用率过高      09-08 10:01    未确认     │
│ [重要] 虚拟机内存不足         09-08 09:55    未确认     │
│ [警告] ...                    ...             已确认     │
└───────────────────────────────────────────────────────┘
```
- 列表行按 eventLevel 配色（紧急红 / 重要橙 / 次要黄 / 警告灰蓝）。
- 双击或点击「AI分析」弹出「告警详情 + AI 建议」弹窗（见 4.4）。

### 4.3 模块二 桌面健康监控（内容区）
```
┌──────────────────────────────────────────────────────┐
│ [一键刷新] 最后刷新：09-08 10:10    │
├──────────────────────────────────────────────────────┤
│ 名称 │ IP │ 系统 │ 状态 │ 宿主CPU% │ 内存% │ 磁盘% │
│ 桌面A│...│Win10│运行 │ 92🔴     │ 70   │ 55    │
│ 桌面B│...│Win11│关机 │ 12       │ 20   │ 30    │
└──────────────────────────────────────────────────────┘
```
- CPU/内存 ≥85% 单元格红色高亮。

### 4.4 关键弹窗

**（a）告警详情 + AI 建议弹窗**
```
┌─ 告警详情 ────────────────────────────────[x]─┐
│ 名称：主机CPU使用率过高                         │
│ 级别：紧急(1)   状态：未确认   时间：09-08 10:01│
│ 描述：host-03 CPU 使用率达到 97%               │
├─ AI 分析 ─────────────────────────────────────┤
│ 【故障含义】...                                 │
│ 【排查/解决建议】                               │
│ 1. ...                                          │
│ 2. ...                                          │
│ 3. ...                                          │
└────────────────────────────────────────────────┘
```

**（b）设置页（弹窗 / 独立 Tab）**
```
┌─ 设置 ─────────────────────────────────────[x]─┐
│ H3C 平台                                          │
│   地址: [http://10.1.1.201:8083        ]         │
│   账号: [__________]  密码: [******]             │
│ 大模型(OpenAI兼容)                                │
│   厂商: [DeepSeek▾] base_url:[___________]       │
│   api_key:[****************]  模型:[deepseek-chat]│
│ 知识库                                            │
│   检索方式: [IMA本地检索▾]  本地兜底目录:[knowledge]│
│ [保存] [测试连接]                                 │
└────────────────────────────────────────────────┘
```

**（c）版本更新提示弹窗**
```
┌─ 发现新版本 v1.0.1 ───────────────────[x]─┐
│ 当前 v1.0.0，最新 v1.0.1                    │
│ 更新内容：修复告警刷新问题…                  │
│ [立即下载并更新]   [稍后]                    │
└────────────────────────────────────────────┘
```
- 下载完成后提示「重启以完成更新」，重启后自动加载 `updates/v1.0.1/`。

---

## 5. 待确认问题（务实清单）

1. **IMA 本地检索的具体 IPC/API 路径**：用户选择「通过 ima 技能本地检索」，但 IMA 知识库在本地的暴露方式（CLI 路径、HTTP 端口、还是进程间通信协议）未明确，需架构师/IMA 侧确认接口契约与鉴权方式。
2. **桌面 ID 列表如何获取**：模块二需遍历所有桌面 ID 调用 2.9.11，但需求与实测 API 仅给出「按 id 查概要」，缺少「列出全部桌面」的接口说明。需确认是否有 `list` 类接口，或是否需先调用主机/集群接口再枚举；若必须枚举，需明确顶层入口。
3. **告警自动刷新间隔**：F1-06 提及自动刷新，但默认间隔（如 30s / 60s / 可配置范围）未定，需与运维节奏确认，避免对 REST 服务造成压力。
4. **LLM 默认厂商与模型**：base_url/api_key 配置化已定，但出厂默认指向哪家（DeepSeek / GLM / 通义）及默认 model 名称未定，影响设置页默认填充与首次体验。
5. **RAG 上下文策略**：知识库命中后拼接多少条、单条最大 token、相似度阈值如何设定；本地兜底知识库的文件格式（.md/.txt/.pdf）与向量化方案需确认。
6. **版本更新源**：新版本号检测走「远程接口」还是「指定共享路径/本地文件」，更新包完整性校验（校验和/签名）机制是否需要。
7. **性能查询范围与频率**：模块二需对每台宿主机调用 2.4.12，桌面与宿主机数量规模较大时是否需并发/分页/限流，需结合集群规模确认。
8. **磁盘利用率展示口径**：2.4.12 返回 disk[] 数组（多磁盘），表格「磁盘%」取均值、最大值还是首个关键盘，需明确。

---

*— 本文档为 v1.0.0 简单 PRD，覆盖四大模块全部细则；待确认问题将在评审后回填。*
